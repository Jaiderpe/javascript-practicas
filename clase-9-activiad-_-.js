const form =datos.querySelector("#miForm");

function visualizacion() {
    let formData = new  FormData();
    formData.get("nombre")
    formData.get("edad")
    formData.get("email")

    odjeto.visualizacion = visualizacion
    console.log(odjeto);
};